services: 

  backend:
    build: "./backend"
    ports:
      - "3000:3000"
    volumes:
      - "./backend:/app"
    command: "pnpm dev"
    environment: 
      JWT_SECRET: ${JWT_SECRET}

  mongodb:
    image: "mongo:8"
    volumes:
    - "mongodb_data:/data/db"

volumes:
  mongodb_data:
