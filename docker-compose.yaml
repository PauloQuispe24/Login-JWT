services: 

  backend:
    build: "./backend"
    ports:
      - "3000:3000"
    volumes:
      - "./backend:/app"
    command: "pnpm dev"

  mongodb:
    image: "mongo:8"
    volumes:
    - "mongodb_data:/data/db"

volumes:
  mongodb_data:
